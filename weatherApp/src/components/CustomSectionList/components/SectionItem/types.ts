import { WeatherForFiveDaysListElemType } from '../../../../types';

export type SectionItemPropTypes = {
  index: number;
  item: WeatherForFiveDaysListElemType;
};

export type SectionItemTextInfoPropTypes = {
  rightText: string;
  leftText: string;
};
